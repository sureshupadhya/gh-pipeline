name: 02 Workflow Events
on: 
  push:
  pull_request:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
    
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: WorkFlow Events
        run: echo "i have been trigged by ${{github.event_name}} event"